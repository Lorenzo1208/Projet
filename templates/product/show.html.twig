{% extends 'base.html.twig' %}

{% block title %}
	{{product.name}}
	Boutique
{% endblock %}

{% block content %}
	<link href="{{asset('assets/css/productshow.css')}}" rel="stylesheet">
			<link rel="stylesheet" href="{{asset('assets/css/productshow.css')}}">

	<div class="row">

		<div class="col-md-5"></div>
		<p class="flotte">
			<img src="/uploads/{{ product.illustration}}" alt="{{product.name}}" width="400" height="400" class="img-fluid" float="left">
		</p>
	</div>

	<div class="col-md-7 my-auto"></div>

	<h3>{{product.name}}</h3>
	<p>{{product.subtitle}}</p>
	<span class="product-page-price">{{ (product.price/100) | number_format(2, ',', '.') }}
		€</span>
	<hr>
	<p>
		{{product.description}}
	</p>
	<a href="{{ path('add_to_cart', { 'id' : product.id }) }}" class="btn btn-primary">Ajouter au panier</a>

</div>{% endblock %}
